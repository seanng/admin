import styled from 'styled-components';
import metrics from 'themes/metrics';

const Label = styled.div`
  display: flex;
  width: 50%;
  padding-right: 1.5rem;
  justify-content: flex-end;
  align-items: center;
  font-size: ${metrics.fontSize.h5};
`;

export default Label;
