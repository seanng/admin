import styled from 'styled-components';
import metrics from 'themes/metrics';
import displayStyles from './displayStyles';

const D1 = styled(displayStyles)`
  fontsize: ${metrics.fontSize.d1};
`;

export default D1;
