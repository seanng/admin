import styled from 'styled-components';
import metrics from 'themes/metrics';
import headerStyles from './headerStyles';

const H1 = styled(headerStyles)`
  font-size: ${metrics.fontSize.h1};
`;

export default H1;
