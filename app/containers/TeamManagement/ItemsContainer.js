import styled from 'styled-components';

const ItemsContainer = styled.div`
  display: flex;
  flex: 5;
  flex-direction: row;
  flex-wrap: wrap;
  height: 100%;
  overflow: scroll;
`;

export default ItemsContainer;
