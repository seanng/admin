import styled from 'styled-components';
import colors from 'themes/colors';

const Body = styled.div`
  padding: 60px 0;
  color: ${colors.base3};
  font-weight: 600;
  line-height: 24px;
  text-align: center;
`;

export default Body;
