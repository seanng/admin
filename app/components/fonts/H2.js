import styled from 'styled-components';
import metrics from 'themes/metrics';
import headerStyles from './headerStyles';

const H2 = styled(headerStyles)`
  fontSize: ${metrics.fontSize.h2};
`;

export default H2;
