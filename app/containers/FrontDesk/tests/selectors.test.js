// import { fromJS } from 'immutable';
// import { makeSelectFrontDeskDomain } from '../selectors';

// const selector = makeSelectFrontDeskDomain();

describe('makeSelectFrontDeskDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
