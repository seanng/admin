// import { fromJS } from 'immutable';
// import { makeSelectPastStaysDomain } from '../selectors';

// const selector = makeSelectPastStaysDomain();

describe('makeSelectPastStaysDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
