// import { fromJS } from 'immutable';
// import { makeSelectHotelProfileDomain } from '../selectors';

// const selector = makeSelectHotelProfileDomain();

describe('makeSelectHotelProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
