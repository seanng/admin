import styled from 'styled-components';
import metrics from 'themes/metrics';
import displayStyles from './displayStyles';

const D3 = styled(displayStyles)`
  fontSize: ${metrics.fontSize.d3};
`;

export default D3;
