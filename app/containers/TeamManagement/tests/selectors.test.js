// import { fromJS } from 'immutable';
// import { makeSelectTeamManagementDomain } from '../selectors';

// const selector = makeSelectTeamManagementDomain();

describe('makeSelectTeamManagementDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
